// Code generated by protoc-gen-ts_proto. DO NOT EDIT.
// versions:
//   protoc-gen-ts_proto  v2.7.0
//   protoc               v6.30.2
// source: aircraft.proto

/* eslint-disable */
import { BinaryReader, BinaryWriter } from "@bufbuild/protobuf/wire";
import { GrpcMethod, GrpcStreamMethod } from "@nestjs/microservices";
import { Observable } from "rxjs";
import { PageInfo, PaginationRequest } from "./common";

export const protobufPackage = "airline.aircraft";

export interface GetAllAircraftsRequest {
  pagination: PaginationRequest | undefined;
}

export interface AircraftByCodeRequest {
  aircraftCode: string;
}

export interface Aircraft {
  aircraftCode: string;
  model: string;
  range: number;
}

export interface AircraftList {
  aircrafts: Aircraft[];
}

export interface PaginatedAircraftList {
  aircrafts: Aircraft[];
  pageInfo: PageInfo | undefined;
}

export const AIRLINE_AIRCRAFT_PACKAGE_NAME = "airline.aircraft";

function createBaseGetAllAircraftsRequest(): GetAllAircraftsRequest {
  return { pagination: undefined };
}

export const GetAllAircraftsRequest: MessageFns<GetAllAircraftsRequest> = {
  encode(message: GetAllAircraftsRequest, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.pagination !== undefined) {
      PaginationRequest.encode(message.pagination, writer.uint32(10).fork()).join();
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): GetAllAircraftsRequest {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    let end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseGetAllAircraftsRequest();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.pagination = PaginationRequest.decode(reader, reader.uint32());
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },
};

function createBaseAircraftByCodeRequest(): AircraftByCodeRequest {
  return { aircraftCode: "" };
}

export const AircraftByCodeRequest: MessageFns<AircraftByCodeRequest> = {
  encode(message: AircraftByCodeRequest, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.aircraftCode !== "") {
      writer.uint32(10).string(message.aircraftCode);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): AircraftByCodeRequest {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    let end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseAircraftByCodeRequest();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.aircraftCode = reader.string();
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },
};

function createBaseAircraft(): Aircraft {
  return { aircraftCode: "", model: "", range: 0 };
}

export const Aircraft: MessageFns<Aircraft> = {
  encode(message: Aircraft, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.aircraftCode !== "") {
      writer.uint32(10).string(message.aircraftCode);
    }
    if (message.model !== "") {
      writer.uint32(18).string(message.model);
    }
    if (message.range !== 0) {
      writer.uint32(24).int32(message.range);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): Aircraft {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    let end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseAircraft();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.aircraftCode = reader.string();
          continue;
        }
        case 2: {
          if (tag !== 18) {
            break;
          }

          message.model = reader.string();
          continue;
        }
        case 3: {
          if (tag !== 24) {
            break;
          }

          message.range = reader.int32();
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },
};

function createBaseAircraftList(): AircraftList {
  return { aircrafts: [] };
}

export const AircraftList: MessageFns<AircraftList> = {
  encode(message: AircraftList, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    for (const v of message.aircrafts) {
      Aircraft.encode(v!, writer.uint32(10).fork()).join();
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): AircraftList {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    let end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseAircraftList();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.aircrafts.push(Aircraft.decode(reader, reader.uint32()));
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },
};

function createBasePaginatedAircraftList(): PaginatedAircraftList {
  return { aircrafts: [], pageInfo: undefined };
}

export const PaginatedAircraftList: MessageFns<PaginatedAircraftList> = {
  encode(message: PaginatedAircraftList, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    for (const v of message.aircrafts) {
      Aircraft.encode(v!, writer.uint32(10).fork()).join();
    }
    if (message.pageInfo !== undefined) {
      PageInfo.encode(message.pageInfo, writer.uint32(18).fork()).join();
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): PaginatedAircraftList {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    let end = length === undefined ? reader.len : reader.pos + length;
    const message = createBasePaginatedAircraftList();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.aircrafts.push(Aircraft.decode(reader, reader.uint32()));
          continue;
        }
        case 2: {
          if (tag !== 18) {
            break;
          }

          message.pageInfo = PageInfo.decode(reader, reader.uint32());
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },
};

/** Aircraft Service */

export interface AircraftServiceClient {
  getAllAircrafts(request: GetAllAircraftsRequest, ...rest: any): Observable<PaginatedAircraftList>;

  getAircraftByCode(request: AircraftByCodeRequest, ...rest: any): Observable<Aircraft>;
}

/** Aircraft Service */

export interface AircraftServiceController {
  getAllAircrafts(
    request: GetAllAircraftsRequest,
    ...rest: any
  ): Promise<PaginatedAircraftList> | Observable<PaginatedAircraftList> | PaginatedAircraftList;

  getAircraftByCode(request: AircraftByCodeRequest, ...rest: any): Promise<Aircraft> | Observable<Aircraft> | Aircraft;
}

export function AircraftServiceControllerMethods() {
  return function (constructor: Function) {
    const grpcMethods: string[] = ["getAllAircrafts", "getAircraftByCode"];
    for (const method of grpcMethods) {
      const descriptor: any = Reflect.getOwnPropertyDescriptor(constructor.prototype, method);
      GrpcMethod("AircraftService", method)(constructor.prototype[method], method, descriptor);
    }
    const grpcStreamMethods: string[] = [];
    for (const method of grpcStreamMethods) {
      const descriptor: any = Reflect.getOwnPropertyDescriptor(constructor.prototype, method);
      GrpcStreamMethod("AircraftService", method)(constructor.prototype[method], method, descriptor);
    }
  };
}

export const AIRCRAFT_SERVICE_NAME = "AircraftService";

export interface MessageFns<T> {
  encode(message: T, writer?: BinaryWriter): BinaryWriter;
  decode(input: BinaryReader | Uint8Array, length?: number): T;
}
