// Code generated by protoc-gen-ts_proto. DO NOT EDIT.
// versions:
//   protoc-gen-ts_proto  v2.7.0
//   protoc               v6.30.2
// source: src/grpc/proto/flight/flight.proto

/* eslint-disable */
import { BinaryReader, BinaryWriter } from "@bufbuild/protobuf/wire";
import { GrpcMethod, GrpcStreamMethod } from "@nestjs/microservices";
import { Observable } from "rxjs";
import { Empty } from "../common/common";

export const protobufPackage = "airline.flight";

export interface FlightByIdRequest {
  id: number;
}

export interface Flight {
  id: number;
  flightNo: string;
  scheduledDeparture: string;
  scheduledArrival: string;
  departureAirport: string;
  arrivalAirport: string;
  status: string;
  aircraftCode: string;
  actualDeparture: number;
  actualArrival: number;
}

export interface FlightList {
  flights: Flight[];
}

export interface FlightEvent {
  flight: Flight | undefined;
}

export interface FlightStatusEvent {
  flight: Flight | undefined;
  previousStatus: string;
  newStatus: string;
}

export const AIRLINE_FLIGHT_PACKAGE_NAME = "airline.flight";

function createBaseFlightByIdRequest(): FlightByIdRequest {
  return { id: 0 };
}

export const FlightByIdRequest: MessageFns<FlightByIdRequest> = {
  encode(message: FlightByIdRequest, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.id !== 0) {
      writer.uint32(8).int32(message.id);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): FlightByIdRequest {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    let end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseFlightByIdRequest();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 8) {
            break;
          }

          message.id = reader.int32();
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },
};

function createBaseFlight(): Flight {
  return {
    id: 0,
    flightNo: "",
    scheduledDeparture: "",
    scheduledArrival: "",
    departureAirport: "",
    arrivalAirport: "",
    status: "",
    aircraftCode: "",
    actualDeparture: 0,
    actualArrival: 0,
  };
}

export const Flight: MessageFns<Flight> = {
  encode(message: Flight, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.id !== 0) {
      writer.uint32(8).int32(message.id);
    }
    if (message.flightNo !== "") {
      writer.uint32(18).string(message.flightNo);
    }
    if (message.scheduledDeparture !== "") {
      writer.uint32(26).string(message.scheduledDeparture);
    }
    if (message.scheduledArrival !== "") {
      writer.uint32(34).string(message.scheduledArrival);
    }
    if (message.departureAirport !== "") {
      writer.uint32(42).string(message.departureAirport);
    }
    if (message.arrivalAirport !== "") {
      writer.uint32(50).string(message.arrivalAirport);
    }
    if (message.status !== "") {
      writer.uint32(58).string(message.status);
    }
    if (message.aircraftCode !== "") {
      writer.uint32(66).string(message.aircraftCode);
    }
    if (message.actualDeparture !== 0) {
      writer.uint32(77).float(message.actualDeparture);
    }
    if (message.actualArrival !== 0) {
      writer.uint32(85).float(message.actualArrival);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): Flight {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    let end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseFlight();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 8) {
            break;
          }

          message.id = reader.int32();
          continue;
        }
        case 2: {
          if (tag !== 18) {
            break;
          }

          message.flightNo = reader.string();
          continue;
        }
        case 3: {
          if (tag !== 26) {
            break;
          }

          message.scheduledDeparture = reader.string();
          continue;
        }
        case 4: {
          if (tag !== 34) {
            break;
          }

          message.scheduledArrival = reader.string();
          continue;
        }
        case 5: {
          if (tag !== 42) {
            break;
          }

          message.departureAirport = reader.string();
          continue;
        }
        case 6: {
          if (tag !== 50) {
            break;
          }

          message.arrivalAirport = reader.string();
          continue;
        }
        case 7: {
          if (tag !== 58) {
            break;
          }

          message.status = reader.string();
          continue;
        }
        case 8: {
          if (tag !== 66) {
            break;
          }

          message.aircraftCode = reader.string();
          continue;
        }
        case 9: {
          if (tag !== 77) {
            break;
          }

          message.actualDeparture = reader.float();
          continue;
        }
        case 10: {
          if (tag !== 85) {
            break;
          }

          message.actualArrival = reader.float();
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },
};

function createBaseFlightList(): FlightList {
  return { flights: [] };
}

export const FlightList: MessageFns<FlightList> = {
  encode(message: FlightList, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    for (const v of message.flights) {
      Flight.encode(v!, writer.uint32(10).fork()).join();
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): FlightList {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    let end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseFlightList();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.flights.push(Flight.decode(reader, reader.uint32()));
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },
};

function createBaseFlightEvent(): FlightEvent {
  return { flight: undefined };
}

export const FlightEvent: MessageFns<FlightEvent> = {
  encode(message: FlightEvent, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.flight !== undefined) {
      Flight.encode(message.flight, writer.uint32(10).fork()).join();
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): FlightEvent {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    let end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseFlightEvent();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.flight = Flight.decode(reader, reader.uint32());
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },
};

function createBaseFlightStatusEvent(): FlightStatusEvent {
  return { flight: undefined, previousStatus: "", newStatus: "" };
}

export const FlightStatusEvent: MessageFns<FlightStatusEvent> = {
  encode(message: FlightStatusEvent, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.flight !== undefined) {
      Flight.encode(message.flight, writer.uint32(10).fork()).join();
    }
    if (message.previousStatus !== "") {
      writer.uint32(18).string(message.previousStatus);
    }
    if (message.newStatus !== "") {
      writer.uint32(26).string(message.newStatus);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): FlightStatusEvent {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    let end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseFlightStatusEvent();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.flight = Flight.decode(reader, reader.uint32());
          continue;
        }
        case 2: {
          if (tag !== 18) {
            break;
          }

          message.previousStatus = reader.string();
          continue;
        }
        case 3: {
          if (tag !== 26) {
            break;
          }

          message.newStatus = reader.string();
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },
};

/** Flight Service */

export interface FlightServiceClient {
  getAllFlights(request: Empty, ...rest: any): Observable<FlightList>;

  getFlightById(request: FlightByIdRequest, ...rest: any): Observable<Flight>;

  streamFlightCreated(request: Empty, ...rest: any): Observable<FlightEvent>;

  streamFlightUpdated(request: Empty, ...rest: any): Observable<FlightEvent>;

  streamFlightStatusChanged(request: Empty, ...rest: any): Observable<FlightStatusEvent>;
}

/** Flight Service */

export interface FlightServiceController {
  getAllFlights(request: Empty, ...rest: any): Promise<FlightList> | Observable<FlightList> | FlightList;

  getFlightById(request: FlightByIdRequest, ...rest: any): Promise<Flight> | Observable<Flight> | Flight;

  streamFlightCreated(request: Empty, ...rest: any): Observable<FlightEvent>;

  streamFlightUpdated(request: Empty, ...rest: any): Observable<FlightEvent>;

  streamFlightStatusChanged(request: Empty, ...rest: any): Observable<FlightStatusEvent>;
}

export function FlightServiceControllerMethods() {
  return function (constructor: Function) {
    const grpcMethods: string[] = [
      "getAllFlights",
      "getFlightById",
      "streamFlightCreated",
      "streamFlightUpdated",
      "streamFlightStatusChanged",
    ];
    for (const method of grpcMethods) {
      const descriptor: any = Reflect.getOwnPropertyDescriptor(constructor.prototype, method);
      GrpcMethod("FlightService", method)(constructor.prototype[method], method, descriptor);
    }
    const grpcStreamMethods: string[] = [];
    for (const method of grpcStreamMethods) {
      const descriptor: any = Reflect.getOwnPropertyDescriptor(constructor.prototype, method);
      GrpcStreamMethod("FlightService", method)(constructor.prototype[method], method, descriptor);
    }
  };
}

export const FLIGHT_SERVICE_NAME = "FlightService";

export interface MessageFns<T> {
  encode(message: T, writer?: BinaryWriter): BinaryWriter;
  decode(input: BinaryReader | Uint8Array, length?: number): T;
}
