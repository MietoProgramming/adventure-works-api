// Code generated by protoc-gen-ts_proto. DO NOT EDIT.
// versions:
//   protoc-gen-ts_proto  v2.7.0
//   protoc               v6.30.2
// source: src/grpc/proto/ticket-flight/ticket-flight.proto

/* eslint-disable */
import { BinaryReader, BinaryWriter } from "@bufbuild/protobuf/wire";
import { GrpcMethod, GrpcStreamMethod } from "@nestjs/microservices";
import { Observable } from "rxjs";
import { Empty } from "../common/common";

export const protobufPackage = "airline.ticketflight";

export interface TicketFlightByTicketAndFlightIdRequest {
  ticketNo: string;
  flightId: number;
}

export interface TicketFlight {
  ticketNo: string;
  flightId: number;
  fareConditions: string;
  amount: number;
}

export interface TicketFlightList {
  ticketFlights: TicketFlight[];
}

export const AIRLINE_TICKETFLIGHT_PACKAGE_NAME = "airline.ticketflight";

function createBaseTicketFlightByTicketAndFlightIdRequest(): TicketFlightByTicketAndFlightIdRequest {
  return { ticketNo: "", flightId: 0 };
}

export const TicketFlightByTicketAndFlightIdRequest: MessageFns<TicketFlightByTicketAndFlightIdRequest> = {
  encode(message: TicketFlightByTicketAndFlightIdRequest, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.ticketNo !== "") {
      writer.uint32(10).string(message.ticketNo);
    }
    if (message.flightId !== 0) {
      writer.uint32(16).int32(message.flightId);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): TicketFlightByTicketAndFlightIdRequest {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    let end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseTicketFlightByTicketAndFlightIdRequest();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.ticketNo = reader.string();
          continue;
        }
        case 2: {
          if (tag !== 16) {
            break;
          }

          message.flightId = reader.int32();
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },
};

function createBaseTicketFlight(): TicketFlight {
  return { ticketNo: "", flightId: 0, fareConditions: "", amount: 0 };
}

export const TicketFlight: MessageFns<TicketFlight> = {
  encode(message: TicketFlight, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.ticketNo !== "") {
      writer.uint32(10).string(message.ticketNo);
    }
    if (message.flightId !== 0) {
      writer.uint32(16).int32(message.flightId);
    }
    if (message.fareConditions !== "") {
      writer.uint32(26).string(message.fareConditions);
    }
    if (message.amount !== 0) {
      writer.uint32(37).float(message.amount);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): TicketFlight {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    let end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseTicketFlight();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.ticketNo = reader.string();
          continue;
        }
        case 2: {
          if (tag !== 16) {
            break;
          }

          message.flightId = reader.int32();
          continue;
        }
        case 3: {
          if (tag !== 26) {
            break;
          }

          message.fareConditions = reader.string();
          continue;
        }
        case 4: {
          if (tag !== 37) {
            break;
          }

          message.amount = reader.float();
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },
};

function createBaseTicketFlightList(): TicketFlightList {
  return { ticketFlights: [] };
}

export const TicketFlightList: MessageFns<TicketFlightList> = {
  encode(message: TicketFlightList, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    for (const v of message.ticketFlights) {
      TicketFlight.encode(v!, writer.uint32(10).fork()).join();
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): TicketFlightList {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    let end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseTicketFlightList();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.ticketFlights.push(TicketFlight.decode(reader, reader.uint32()));
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },
};

/** TicketFlight Service */

export interface TicketFlightServiceClient {
  getAllTicketFlights(request: Empty, ...rest: any): Observable<TicketFlightList>;

  getTicketFlightByTicketAndFlightId(
    request: TicketFlightByTicketAndFlightIdRequest,
    ...rest: any
  ): Observable<TicketFlight>;
}

/** TicketFlight Service */

export interface TicketFlightServiceController {
  getAllTicketFlights(
    request: Empty,
    ...rest: any
  ): Promise<TicketFlightList> | Observable<TicketFlightList> | TicketFlightList;

  getTicketFlightByTicketAndFlightId(
    request: TicketFlightByTicketAndFlightIdRequest,
    ...rest: any
  ): Promise<TicketFlight> | Observable<TicketFlight> | TicketFlight;
}

export function TicketFlightServiceControllerMethods() {
  return function (constructor: Function) {
    const grpcMethods: string[] = ["getAllTicketFlights", "getTicketFlightByTicketAndFlightId"];
    for (const method of grpcMethods) {
      const descriptor: any = Reflect.getOwnPropertyDescriptor(constructor.prototype, method);
      GrpcMethod("TicketFlightService", method)(constructor.prototype[method], method, descriptor);
    }
    const grpcStreamMethods: string[] = [];
    for (const method of grpcStreamMethods) {
      const descriptor: any = Reflect.getOwnPropertyDescriptor(constructor.prototype, method);
      GrpcStreamMethod("TicketFlightService", method)(constructor.prototype[method], method, descriptor);
    }
  };
}

export const TICKET_FLIGHT_SERVICE_NAME = "TicketFlightService";

export interface MessageFns<T> {
  encode(message: T, writer?: BinaryWriter): BinaryWriter;
  decode(input: BinaryReader | Uint8Array, length?: number): T;
}
