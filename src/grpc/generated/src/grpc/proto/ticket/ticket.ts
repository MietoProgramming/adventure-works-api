// Code generated by protoc-gen-ts_proto. DO NOT EDIT.
// versions:
//   protoc-gen-ts_proto  v2.7.0
//   protoc               v6.30.2
// source: src/grpc/proto/ticket/ticket.proto

/* eslint-disable */
import { BinaryReader, BinaryWriter } from "@bufbuild/protobuf/wire";
import { GrpcMethod, GrpcStreamMethod } from "@nestjs/microservices";
import { Observable } from "rxjs";
import { Empty } from "../common/common";

export const protobufPackage = "airline.ticket";

export interface TicketByNoRequest {
  ticketNo: string;
}

export interface Ticket {
  ticketNo: string;
  bookRef: string;
  passengerId: string;
  passengerName: string;
  contactData: string;
}

export interface TicketList {
  tickets: Ticket[];
}

export const AIRLINE_TICKET_PACKAGE_NAME = "airline.ticket";

function createBaseTicketByNoRequest(): TicketByNoRequest {
  return { ticketNo: "" };
}

export const TicketByNoRequest: MessageFns<TicketByNoRequest> = {
  encode(message: TicketByNoRequest, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.ticketNo !== "") {
      writer.uint32(10).string(message.ticketNo);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): TicketByNoRequest {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    let end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseTicketByNoRequest();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.ticketNo = reader.string();
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },
};

function createBaseTicket(): Ticket {
  return { ticketNo: "", bookRef: "", passengerId: "", passengerName: "", contactData: "" };
}

export const Ticket: MessageFns<Ticket> = {
  encode(message: Ticket, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.ticketNo !== "") {
      writer.uint32(10).string(message.ticketNo);
    }
    if (message.bookRef !== "") {
      writer.uint32(18).string(message.bookRef);
    }
    if (message.passengerId !== "") {
      writer.uint32(26).string(message.passengerId);
    }
    if (message.passengerName !== "") {
      writer.uint32(34).string(message.passengerName);
    }
    if (message.contactData !== "") {
      writer.uint32(42).string(message.contactData);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): Ticket {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    let end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseTicket();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.ticketNo = reader.string();
          continue;
        }
        case 2: {
          if (tag !== 18) {
            break;
          }

          message.bookRef = reader.string();
          continue;
        }
        case 3: {
          if (tag !== 26) {
            break;
          }

          message.passengerId = reader.string();
          continue;
        }
        case 4: {
          if (tag !== 34) {
            break;
          }

          message.passengerName = reader.string();
          continue;
        }
        case 5: {
          if (tag !== 42) {
            break;
          }

          message.contactData = reader.string();
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },
};

function createBaseTicketList(): TicketList {
  return { tickets: [] };
}

export const TicketList: MessageFns<TicketList> = {
  encode(message: TicketList, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    for (const v of message.tickets) {
      Ticket.encode(v!, writer.uint32(10).fork()).join();
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): TicketList {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    let end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseTicketList();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.tickets.push(Ticket.decode(reader, reader.uint32()));
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },
};

/** Ticket Service */

export interface TicketServiceClient {
  getAllTickets(request: Empty, ...rest: any): Observable<TicketList>;

  getTicketByNo(request: TicketByNoRequest, ...rest: any): Observable<Ticket>;
}

/** Ticket Service */

export interface TicketServiceController {
  getAllTickets(request: Empty, ...rest: any): Promise<TicketList> | Observable<TicketList> | TicketList;

  getTicketByNo(request: TicketByNoRequest, ...rest: any): Promise<Ticket> | Observable<Ticket> | Ticket;
}

export function TicketServiceControllerMethods() {
  return function (constructor: Function) {
    const grpcMethods: string[] = ["getAllTickets", "getTicketByNo"];
    for (const method of grpcMethods) {
      const descriptor: any = Reflect.getOwnPropertyDescriptor(constructor.prototype, method);
      GrpcMethod("TicketService", method)(constructor.prototype[method], method, descriptor);
    }
    const grpcStreamMethods: string[] = [];
    for (const method of grpcStreamMethods) {
      const descriptor: any = Reflect.getOwnPropertyDescriptor(constructor.prototype, method);
      GrpcStreamMethod("TicketService", method)(constructor.prototype[method], method, descriptor);
    }
  };
}

export const TICKET_SERVICE_NAME = "TicketService";

export interface MessageFns<T> {
  encode(message: T, writer?: BinaryWriter): BinaryWriter;
  decode(input: BinaryReader | Uint8Array, length?: number): T;
}
